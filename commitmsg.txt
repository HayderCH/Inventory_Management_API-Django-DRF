feat(api): enterprise-grade ProductSupplier ViewSet with audit logging, action-based permissions, and optimized query performance

- Implements action-based permissions (admin, manager, employee, auditor)
- Adds full audit logging for create, update, and delete operations
- Uses select_related for product and supplier to prevent N+1 queries
- Supports filtering, searching, and ordering on all key business fields
- Separates serializers for list, detail, and write actions
- Ready for extension with bulk operations and further business logic